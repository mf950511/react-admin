.icon {
  width: 1em;
  height: 1em;
  vertical-align: -0.15em;
  fill: currentColor;
  overflow: hidden;
}
html {
  line-height: 1.15;
}
.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}
